# spring security 配置
security:
  oauth2:
    client:
      client-id: dips
      client-secret: dips
      scope: server
debug: false
logging:
  level:
    org:
      springframework:
        web: INFO
    com:
      cloud:
        gds:
          cleaning: DEBUG

# 数据源配置
spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: gov@20130528
#    password: 12345678
    #本地数据库
#    url: jdbc:mysql://127.0.0.1:3306/data-clean?characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false
    url: jdbc:mysql://115.233.227.46:13308/data-clean?characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false

# swagger相关配置,覆盖全局配置
swagger:
  authorization:
    authorization-scope-list:
    -   scope: 'server'
        description: 'server all'
    -   scope: 'read'
        description: 'read all'
    -   scope: 'write'
        description: 'access all'

file-save:
  path: E:\WorkSpeace\test
